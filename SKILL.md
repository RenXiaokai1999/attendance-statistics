---
name: attendance-statistics
description: 每月汇总大同台考勤。用于“统计上月考勤”“生成考勤表”“重跑某月考勤”等请求；读取签卡、24h值班、数据预处理和企业微信图片，生成旧版Word考勤表及带颜色的Excel副本。
---

# 考勤统计

## 运行方式

1. 默认目标为当前日期的上一个自然月；用户指定月份时以指定值为准。
2. 加载 `config/settings.json`，凭据只从 Windows 凭据管理器中的 `credential_target` 读取。
3. 预检模板、年度工作日日历、当月24h值班目录和数据预处理目录；缺少任何必需项立即停止并报告。
4. 从考勤网站下载整月签卡 `.xlsx` 到新建的月份目录。
5. 解析签卡、零报告、预处理 HTML，以及目标月、前一月和后一月企业微信图片；只保留目标月内记录。水准测量固定核对每月1日、6日、11日、16日、21日、26日，缺一日就列入人工确认；31日不属于固定水准测量日。
6. 对图片候选进行视觉复核。凡正式“用车申请”“用车审批单”或“派车申请”，无论事由、审批状态以及是否撤回、取消、作废，均先纳入因公外出，由用户最终手动甄别删除；单独的出租车、打车、网约车票据不统计。其他事项的未批准、低置信度、半天/小时和冲突项不自动填写，统一报告。
7. 将统计写入另存的 `.doc`；新增人员放到名单末尾，未知字段留空。因公外出按目标月内自然日统计，工作日、周末和法定节假日均计入。用车单中明确列出的全部使用人分别计数；同一事项只写一条明细，申请人为登记人，其他使用人写入同行人员，多张附件归并去重。清空第二张登记表的模板示例，并按每个最终有效事项一行写入日期、申请人、事由地点、起止时间、自然日天数、批准人和同行人员。
8. 将每日状态着色后另存为 `_考勤标注.xlsx`，不得覆盖原始签卡文件。
9. 重新打开输出并验证；成功后报告统计摘要和待人工确认项。

## 失败与重跑

- 定时运行失败时只通知，不自动重试。
- 用户补齐资料后，可说“重跑 YYYY年M月考勤”手动触发。
- 详细判定遵循 `references/统计规则.md`；年度日历遵循 `references/法定节假日数据说明.md`。

## 安全要求

- 不在代码、配置、日志或报告中显示账户名、密码及其他秘密。
- 所有来源只读；写入前先复制模板或原始工作簿。
