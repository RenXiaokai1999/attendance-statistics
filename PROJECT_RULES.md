# 考勤统计 Skill 项目规则

## 固定路径

- 考勤根目录：`E:\山西省地震局工作\大同台\考勤`
- 手动资料根目录：`E:\山西省地震局工作\大同台\考勤\24h值班和预处理`
- 企业微信图片根目录：`C:\Users\11789\Documents\WXWork\1688854483903422\Cache\Image`
- Word 模板：`E:\山西省地震局工作\大同台\考勤\2026年8月测试模板\202608DTZ考勤表.doc`

## 安全规则

- 登录信息只保存在 Windows 凭据管理器。
- Skill、脚本、日志、测试数据和定时任务提示词不得包含明文密码。
- 不覆盖原始 Word、Excel、图片、HTML或零报告。
- 旧版 `.doc` 通过本机 WPS/Word COM 处理；传入 COM 的路径必须是绝对路径，退出时保持锚点文档打开并直接退出应用。
- 目标输出被占用时不得强行关闭用户程序，改为另存并提醒用户手动重跑。

## 通用数据规则

- 目标月份为运行日期的上一个自然月。
- 图片读取目标月份、前一个月和后一个月三个目录；最终只统计目标月内日期。
- 姓名只做确定性规范化，不进行无依据的模糊合并。
- 模板中不存在的人员追加到名单末尾，仅填写姓名和已有统计，其他项目留空。
- 无数据的统计格保持空白，不填写 `0`。
- 同一人员、日期和类型的重复记录只计一次。
- 撤回、取消或改期记录以最新状态为准；正式用车申请例外，仍先统计并由用户手动删除。
- 无法确认、半天、小时数及相互冲突的记录暂不计入，列入提醒。

## 各项统计规则

### 出勤

- 同一人员当天有任意有效打卡记录即计1天。

### 全日班

- 每份零报告末尾的“填报人”计1次。
- 同一天存在不同填报人时标记冲突，该日不计。

### 白班

- 合并 HTML 分页后读取“日期”和“值班员”。
- 只统计正常休息性质的自然周六、周日。
- 法定节假日内的周末不计；调休上班的周末也不计。
- 同一天存在不同值班员时标记冲突，该日不计。

### 补休

- 只统计明确获批、日期属于目标月份的调休。
- 只有申请、没有批准结果的记录不计并提醒。

### 请假

- 统计明确获批的年假、育儿假、探亲假、婚假、病假、事假等非调休休假。
- 跨月记录只计目标月份内的部分。
- 仅有起止日期时按国家工作日计算；法定假期不计，调休上班的周末计入。

### 因公外出

- 只统计明确获批的因公出差、因公外出办事，以及经组织同意参加的学习培训。
- 凡正式“用车申请”“用车审批单”或“派车申请”，不判断事由和审批状态，一律先统计为因公外出；未审批、驳回、撤回、取消、作废也统计，最终由用户手动甄别删除。
- 正式用车申请优先于出租车、打车、网约车排除词；单独的出租车、打车、网约车票据不统计。
- 用车单明确列出的全部使用人按自然日分别计数；同一事项的申请单、审批页、发票和附件按事项、人员、日期归并去重。
- 每个用车事项在第二张登记表只写一行，申请人作为登记人，其他明确使用人写入同行人员。
- 租车材料属于独立有效依据，不要求另有审批截图；只要能明确确认用车日期和人员，即纳入因公外出。
- “出租车”“打车”“网约车”属于即时客运，不计入因公外出；排除规则优先于租车关键词。
- 租车材料只有承租人时只计承租人，不推测同行人员；日期或人员不清时进入人工确认。
- 同一租车事项的合同、发票、订单和明细按事项、人员、日期归并去重。
- 水准测量归入因公外出办事。
- 水准测量固定核对每月1日、6日、11日、16日、21日、26日；任一日期没有已确认记录时必须提醒人工复核。31日不属于固定水准测量日。
- 单独附件未出现正式用车申请时，租车材料仍按既有独立证据规则处理；其他普通用车附件不自动统计。
- 按目标月内自然日计算；工作日、周末和法定节假日均计入。

### 因公外出明细登记

- 生成当月 Word 时，清空第二张“因公外出、请（休）假和缺勤情况登记表”的模板示例行。
- 每个最终有效的已批准事项写一行；申请人填登记人，其他参加人员写入备注的同行人员。
- 写明事项日期、事由及地点、原始起止日期、目标月内自然日天数、批准人和同行人员。
- 批准人无法可靠识别时保持空白，不猜测。
- 第一张表汇总、第二张表明细和 Excel 着色必须使用同一批归并后的最终记录。

## Excel 着色规则

- 有打卡：无底色。
- 无打卡但有全日班或白班：无底色。
- 因公外出：深蓝色 `#002060`。
- 补休：浅绿色 `#92D050`。
- 请假：黑色 `#000000`。
- 无任何有效状态且缺卡：红色 `#FF0000`。
- 同日存在多个互斥状态：不着色并列入冲突清单。
- 有打卡同时存在外出或休假状态：无底色并提醒复核。

## 失败处理

- 缺少当月 `24h值班` 或 `数据预处理` 目录时立即停止并提醒。
- 登录或下载失败时停止，不生成不完整结果。
- 单个文件异常时继续处理其他数据，并列入异常清单。
- 自动任务失败后等待用户手动重跑。

## 定时任务

- Codex 自动任务名称：`每月考勤统计`。
- 每月1日15:00（Asia/Hong_Kong）运行，统计上一个自然月。
- 失败只通知，不自动重试；用户补齐文件或关闭占用程序后手动重跑。

## 版本发布

- GitHub 仓库：`https://github.com/RenXiaokai1999/attendance-statistics`。
- 仓库保持私有，避免公开人员考勤信息和本机环境细节。
- 本地运行结果、企业微信图片、原始签卡表、生成的 Word/Excel 和缓存文件不得提交。
- 首个正式版本名称为 `v1.0`。
