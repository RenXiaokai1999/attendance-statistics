# 考勤统计 Skill 项目概览

## 项目目标

创建 `attendance-statistics` Skill，每月自动汇总大同地震监测中心站上一个自然月的考勤数据，生成 Word 考勤表和带颜色标注的 Excel 签卡记录。

## 运行方式

- 自动运行：每月1日15:00（北京时间）。
- 手动运行：自动任务失败、资料补齐或需要重跑时由用户触发。
- 自动运行失败后不自动重试。

## 输入范围

1. 汇顺云考勤平台下载的上月 `.xlsx` 原始签卡记录。
2. `24h值班和预处理\YYYYMM\24h值班` 中的每日异常零报告 `.doc`。
3. `24h值班和预处理\YYYYMM\数据预处理\YYYY\M月` 中的 HTML 分页记录。
4. 企业微信图片缓存中目标月份、前一个月和后一个月的图片；记录日期仍裁剪到目标月。
5. 固定的旧版 Word `.doc` 考勤模板。
6. 国家当年度法定节假日及调休工作日安排。

## 输出范围

- `考勤\YYYY年M月\YYYYMMDTZ考勤表.doc`
- `考勤\YYYY年M月\签卡记录YYYY-MM-01到YYYY-MM-月末_考勤标注.xlsx`
- 运行摘要及待确认、冲突、未审批和缺失记录清单。
- Word 第二张“因公外出、请（休）假和缺勤情况登记表”中的当月因公外出事项明细。

## 统计项目

- 出勤（天）
- 全日班
- 白班
- 补休
- 请假（天）
- 因公外出（天，按目标月内自然日统计，含周末和法定节假日）
- 因公外出事项明细（日期、申请人、类别、事由地点、起止时间、自然日天数、批准人、同行人员）
- 所有正式用车申请的预收录清单：不按事由或审批状态过滤，先写入汇总和明细，交由用户最终手动删除。

## 验收标准

- 原始文件不被覆盖或修改。
- Word 保持 `.doc` 格式及原有版式。
- Excel 保留原始打卡时间及原有结构。
- 所有统计按姓名和日期准确归并、去重。
- 不确定数据不静默计入，必须进入提醒清单。
- 凭据不以明文出现在 Skill、脚本、日志或定时任务中。
